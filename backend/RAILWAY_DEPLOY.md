# Deploy en Railway (carpeta backend)

Este servicio se despliega **solo desde esta carpeta**, sin Docker.

En Railway: **Root Directory = `backend`**

Ver guía completa en [`RAILWAY_DEPLOY.md`](../RAILWAY_DEPLOY.md) en la raíz del repo.
